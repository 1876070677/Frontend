import React from 'react';
import styles from './section1.module.css'

function Home() {
    return (
        <section className={`${styles.animated_section} styles.main_page`}>
            <div className={styles.section_content}>
                
            </div>
        </section>
    )
}

export default Home;